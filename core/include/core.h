#ifndef PHANTOM_EDGE_CORE_H
#define PHANTOM_EDGE_CORE_H

// Main function for the core loader logic
// Takes a pointer to the raw DLL data and its size
int CoreLoaderMain(unsigned char* pDllData, unsigned int uiDllSize);

#endif //PHANTOM_EDGE_CORE_H 